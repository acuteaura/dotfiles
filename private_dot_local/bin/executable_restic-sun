#!/usr/bin/env fish
set -x RESTIC_REPOSITORY /run/media/aurelia/Sun/(hostname)
if not set -q RESTIC_PASSWORD
  set -x RESTIC_PASSWORD (op item get "ev5c3vciuc3ajh4f2cpzj2hxom" --field password --reveal)
end
restic $argv

local wezterm = require 'wezterm'
local config = {}

config.default_prog = { '/Users/aurelia/.nix-profile/bin/fish', '-l' }

config.color_scheme = 'Fairyfloss'
config.hide_tab_bar_if_only_one_tab = true

config.font = wezterm.font 'TX-02'
config.font_size = 14

return config
